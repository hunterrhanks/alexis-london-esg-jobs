# ============================================================
# Alexis London ESG Job Board V3.0 - Configuration
# Copy this file to .env and fill in your keys
# ============================================================

# Server port (default: 3000)
PORT=3000

# ---- Job Source API Keys (optional, more sources = more jobs) ----

# Reed.co.uk API key (free, register at https://www.reed.co.uk/developers/Jobseeker)
REED_API_KEY=

# Adzuna API keys (free, register at https://developer.adzuna.com/)
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Jooble API key (free aggregator — indexes LinkedIn, Indeed, Glassdoor & 70+ boards)
# Register at https://jooble.org/api/about
JOOBLE_API_KEY=

# The Muse API key (optional — works without key at 500 req/hr, with key 3600 req/hr)
# Register at https://www.themuse.com/developers/api/v2
MUSE_API_KEY=

# ---- AI Match Scoring & Outreach Kit (optional) ----

# Anthropic API key for AI-powered job scoring, "Why this fits Alexis" summaries,
# and the V3.0 AI Outreach Kit (LinkedIn message + resume bullets).
# Without this, the app uses a built-in heuristic scorer (still works well)
# and generates template-based outreach kits.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ---- Daily Digest Email (optional) ----

# Email address to receive the daily digest
DIGEST_EMAIL=

# Option A: SMTP (Gmail, Outlook, etc.)
# For Gmail, use an App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Option B: SendGrid (overrides SMTP if set)
# SENDGRID_API_KEY=

# ---- Deployment (set by hosting platform) ----

# DATA_DIR: persistent disk path for SQLite database and sponsor cache
# Set automatically on Render (/var/data) — leave blank for local dev
# DATA_DIR=
